"""Setup file for the repository. This is a basic file for a python package.

Even for small projects, you should include these because of editable installs.
The proper configuration for the package is specified in the setup.cfg file.
"""
from setuptools import setup

setup()
